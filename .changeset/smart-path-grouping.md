---
"@eventcatalog/generator-openapi": patch
---

Fix path-prefix grouping: include single-segment paths when other paths share the same prefix, use regex for version segment detection, and output group names as /prefix format
